export const metadata = {
  title: 'Login - Drapey',
  description: 'Sign in to your Drapey account to manage orders, wishlist, and profile.',
};

import LoginPageContent from './content';

export default function LoginPage() {
  return <LoginPageContent />;
}
